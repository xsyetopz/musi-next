catalog sema music_sema SemaDiagKind crates/music_sema/src/diag_catalog_gen.rs

diag AttrDuplicateRepr 3000
  message "`@repr` attribute duplicate"

diag AttrReprRequiresKindString 3001
  message "`@repr` needs string `kind` argument"

diag AttrLayoutArgRequiresName 3002
  message "`@layout` argument needs name"

diag AttrDuplicateLayoutAlign 3003
  message "`@layout align` attribute duplicate"

diag AttrLayoutAlignRequiresU32 3004
  message "`@layout align` needs u32 value"

diag AttrDuplicateLayoutPack 3005
  message "`@layout pack` attribute duplicate"

diag AttrLayoutPackRequiresU32 3006
  message "`@layout pack` needs u32 value"

diag AttrUnknownArg 3007
  message "attribute argument `{argument}` unknown"

diag AttrBuiltinRequiresPlainBindLet 3008
  message "`@musi.builtin` needs plain bind `let` target"

diag AttrBuiltinRequiresNameString 3009
  message "`@musi.builtin` needs string `name` argument"

diag AttrBuiltinRequiresFoundationModule 3010
  message "`@musi.builtin` needs `musi:*` module"

diag AttrBuiltinRequiresExport 3011
  message "`@musi.builtin` needs exported binding"

diag AttrBuiltinUnknownName 3012
  message "`@musi.builtin` name unknown"

diag AttrIntrinsicRequiresNameString 3013
  message "`@musi.intrinsic` needs string `name` argument"

diag AttrIntrinsicRequiresIntrinsicsModule 3014
  message "`@musi.intrinsic` needs `musi:intrinsics` module"

diag AttrIntrinsicRequiresForeignLet 3300
  message "`@musi.intrinsic` needs native `let` target"

diag AttrLinkRequiresForeignLet 3015
  message "`@link` needs native `let` target"

diag AttrDataLayoutRequiresDataTarget 3016
  message "data layout attribute target must be `data`"

diag AttrFrozenRequiresExportedNonOpaqueData 3301
  message "`@frozen` needs exported non-opaque `data` target"

diag AttrHotColdRequiresCallable 3302
  message "`@profile` needs callable target"

diag AttrHotColdConflict 3303
  message "`@profile` level must be `.hot` or `.cold`"

diag AttrDeprecatedRequiresStringValue 3304
  message "`@lifecycle` deprecated payload needs string values"

diag AttrSinceRequiresVersionString 3305
  message "`@lifecycle` `since` needs string value"

diag AttrOpaqueRequiresStructuralExport 3306
  message "`opaque` needs exported structural target"

diag AttrIntrinsicUnknownName 3308
  message "`@musi.intrinsic` name unknown"

diag AttrForeignRequiresForeignLet 3017
  message "native member needs `let` target"

diag AttrLinkRequiresStringValue 3018
  message "`@link` needs string values"

diag AttrWhenRequiresStringValue 3019
  message "`@target` needs string values"

diag AttrWhenRequiresStringList 3020
  message "`@target` needs string list for `feature`"

diag ForeignSignatureRequired 3021
  message "native binding needs signature"

diag InvalidPartialModifier 3120
  message "`partial` target invalid"

diag PartialForeignConflict 3121
  message "`partial` conflicts with native binding"

diag InvalidFfiType 3022
  message "native interface type invalid"

diag LawMustBePure 3023
  message "law must be pure"

diag CollectDuplicateDataVariant 3024
  message "data variant `{variant}` duplicate"
  secondary "data variant `{variant}` previous declaration"

diag DuplicateDataVariantDiscriminant 3025
  message "data variant discriminant `{discriminant}` duplicate"

diag InvalidDataVariantDiscriminant 3026
  message "data variant discriminant `{discriminant}` invalid"

diag CyclicDataVariantDiscriminant 3027
  message "cyclic data variant discriminant"

diag CollectDuplicateShapeMember 3030
  message "shape member `{member}` duplicate"

diag CollectDuplicateShapeLaw 3031
  message "shape law `{law}` duplicate"

diag UnknownExport 3032
  message "export binding `{name}` unknown"

diag SealedShape 3048
  message "sealed shape `{shape}`"
  primary "sealed shape `{shape}` used here"

diag UnknownShape 3049
  message "shape `{shape}` unknown"

diag PlainLetRequiresIrrefutablePattern 3051
  message "plain `let` needs irrefutable pattern"

diag ImportRecordDestructuringRequiresImportRecord 3052
  message "import-record destructuring needs import record value"

diag RecordDestructuringRequiresRecord 3053
  message "record destructuring needs record value"

diag CallableLetRequiresSimpleBindingPattern 3054
  message "callable `let` needs simple binding pattern"

diag ArraySpreadRequiresOneDimensionalArray 3055
  message "array spread needs one-dimensional array"

diag InvalidSpreadSource 3056
  message "spread source `{source}` invalid"
  primary "spread source not valid here"

diag DuplicateRecordField 3058
  message "record field `{field}` duplicate"

diag MissingRecordField 3123
  message "record field `{field}` missing"

diag VariantMissingDataContext 3059
  message "variant constructor needs data type context"

diag VariantConstructorArityMismatch 3060
  message "variant constructor payload count differs from declaration"

diag VariantNamedFieldsRequired 3105
  message "variant payload needs named fields"
  primary "variant needs named payload fields"

diag VariantNamedFieldsUnexpected 3106
  message "variant named fields not allowed"
  primary "variant does not accept named payload fields"

diag DuplicateVariantField 3107
  message "variant field `{field}` duplicate"

diag MissingVariantField 3108
  message "variant field `{field}` missing"

diag UnknownVariantField 3109
  message "variant payload field `{field}` unknown"

diag MixedVariantPayloadStyle 3110
  message "mixed variant payload style"
  primary "variant payload fields must be all named or all positional"

diag InvalidVariantArity 3061
  message "variant payload count expected `{expected}`, found `{found}`"

diag UnknownDataVariant 3062
  message "data variant `{variant}` unknown"

diag RecordLiteralRequiresNamedFields 3063
  message "record literal needs named fields"

diag ArrayLiteralLengthUnknownFromRuntimeSpread 3064
  message "array literal length unknown after runtime spread `{spread}`"

diag ArrayLiteralLengthMismatch 3065
  message "array literal length expected `{expected}`, found `{found}`"

diag SumConstructorArityMismatch 3066
  message "sum constructor payload count differs from sum arm"

diag InvalidIndexArgCount 3067
  message "index argument count expected `{expected}`, found `{found}`"

diag InvalidCallTarget 3068
  message "call target `{target}` expected callable type, found `{found}`"
  primary "`{target}` has type `{found}` here"

diag CallArityMismatch 3069
  message "call `{callee}` expected `{expected}` arguments, found `{found}`"
  primary "call `{callee}` has `{found}` arguments here"

diag CallPositionalAfterNamedArgument 3111
  message "positional call argument after named argument"
  primary "positional call argument cannot follow named argument"
  help "move positional arguments before named arguments"

diag CallSpreadAfterNamedArgument 3112
  message "call spread after named argument"
  primary "call spread cannot follow named argument"
  help "move spread arguments before named arguments"

diag CallNamedArgumentUnknown 3113
  message "named call argument `{argument}` unknown"

diag CallNamedArgumentDuplicate 3114
  message "named call argument `{argument}` duplicate"

diag CallNamedArgumentAlreadyProvided 3115
  message "named call argument already provided"

diag CallNamedArgumentsAfterRuntimeSpread 3116
  message "named call arguments after runtime spread"
  primary "named call arguments cannot follow runtime spread"

diag CallNamedSpreadArgument 3117
  message "named spread call argument"
  primary "call spread cannot be named"

diag UnsafeCallRequiresUnsafeBlock 3118
  message "unsafe block required for call"
  primary "call must appear inside `unsafe` block"

diag PinRequiresUnsafeBlock 3126
  message "unsafe block required for pin action"
  primary "pin action must appear inside `unsafe` block"

diag UnsupportedPinTarget 3124
  message "pin target `{target}` unsupported"
  primary "`{target}` cannot be pinned"

diag PinnedValueEscapes 3125
  message "pinned value `{name}` escapes pin scope"
  primary "`{name}` returned from pin body"

diag InvalidTypeApplication 3070
  message "type application target `{target}` invalid"

diag CallRuntimeSpreadRequiresArrayAny 3072
  message "call runtime spread needs `[]Any`"
  primary "runtime call spread needs `[]Any`"
  help "use `[]Any` for runtime-sized spread"

diag CallSpreadRequiresTupleOrArray 3073
  message "call spread needs tuple or array"
  primary "call spread needs tuple or one-dimensional array"
  help "spread tuple or one-dimensional array instead"

diag DeclarationUsedAsValue 3074
  message "declaration form used as value"

diag TargetGateRejected 3075
  message "target gate rejected item"

diag InvalidIndexTarget 3076
  message "index target `{target}` invalid"
  primary "index target must support indexing"

diag UnknownField 3078
  message "record field `{field}` unknown"

diag AmbiguousDotCallable 3102
  message "dot-callable match ambiguous"

diag DotCallableRequiresMutableReceiver 3103
  message "dot-callable needs mutable receiver"

diag InvalidFieldTarget 3079
  message "field target `{target}` invalid"
  primary "field target not valid here"

diag InvalidRecordUpdateTarget 3081
  message "record update target `{target}` invalid"
  primary "record update target must be record value"

diag MutForbiddenInTypeTestTarget 3082
  message "`mut` not allowed in type test target"

diag MutForbiddenInTypeCastTarget 3083
  message "`mut` not allowed in type cast target"

diag WriteTargetRequiresMut 3084
  message "write target needs `mut`"
  primary "assignment target must be mutable"

diag UnsupportedAssignmentTarget 3088
  message "assignment target `{target}` unsupported"
  primary "assignment target not writable"

diag NumericOperandRequired 3089
  message "operand needs numeric type"

diag BinaryOperatorHasNoExecutableLowering 3090
  message "binary operator has no executable lowering"

diag LogicalOperatorDomainMismatch 3127
  message "operator `{operator}` needs `Bool` or matching `Bits[N]`, found `{left}` and `{right}`"

diag UnaryLogicalOperatorDomainMismatch 3128
  message "operator `{operator}` needs `Bool` or `Bits[N]`, found `{found}`"

diag InvalidBitsWidth 3129
  message "bits width `{width}` expected positive natural literal"

diag TypeMismatch 3091
  message "{subject} expected `{expected}`, found `{found}`"
  primary "{subject} has type `{found}` here"

diag InvalidTypeExpression 3092
  message "type expression `{target}` invalid"

diag TypeApplicationArityMismatch 3093
  message "type application argument count differs from type parameters"

diag ArrayTypeRequiresItem 3094
  message "array type needs item type"

diag AmbiguousVariantTag 3095
  message "variant tag ambiguous"
  primary "variant tag needs type context"
  help "add type annotation"

diag VariantPatternArityMismatch 3096
  message "variant pattern payload count differs from declaration"

diag OrPatternBindersMismatch 3097
  message "or-pattern binders must match"

diag UnsatisfiedConstraint 3098
  message "unsatisfied constraint"

diag AmbiguousConstraintEvidence 3099
  message "constraint evidence ambiguous"

diag ConstrainedNonCallableBinding 3100
  message "non-callable `let` cannot have `where` constraints"

diag ExportedCallableRequiresConcreteConstraints 3101
  message "exported callable needs fully resolved constraints"

diag RuntimeValueInComptimeContext 3122
  message "runtime value in `known` context"
